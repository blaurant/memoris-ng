---
phase: 01-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/domain/production.clj
  - backend/src/infrastructure/xtdb/xtdb_production_repo.clj
  - backend/src/infrastructure/in_memory_repo/mem_production_repo.clj
  - backend/src/application/network_scenarios.clj
  - backend/src/infrastructure/rest_api/network_handler.clj
  - backend/src/infrastructure/rest_api/handler.clj
  - backend/test/application/network_scenarios_test.clj
autonomous: true
requirements: [INFR-01, INFR-03]

must_haves:
  truths:
    - "GET /api/v1/networks/:id/detail returns 200 with network + productions + stats for a public network"
    - "GET /api/v1/networks/:id/detail returns 404 for non-public or missing network"
    - "Response JSON contains zero sensitive fields (no IBAN, PRM, user-id)"
    - "find-by-network-id returns all productions belonging to a given network"
    - "Stats (total-capacity-kwc, energy-mix) are correct even when zero active productions"
  artifacts:
    - path: "backend/src/domain/production.clj"
      provides: "find-by-network-id in ProductionRepo protocol"
      contains: "find-by-network-id"
    - path: "backend/src/infrastructure/xtdb/xtdb_production_repo.clj"
      provides: "XTDB implementation of find-by-network-id"
      contains: "find-by-network-id"
    - path: "backend/src/infrastructure/in_memory_repo/mem_production_repo.clj"
      provides: "In-memory implementation of find-by-network-id"
      contains: "find-by-network-id"
    - path: "backend/src/application/network_scenarios.clj"
      provides: "get-network-detail scenario function"
      contains: "get-network-detail"
    - path: "backend/src/infrastructure/rest_api/network_handler.clj"
      provides: "get-network-detail-handler + route"
      contains: "get-network-detail-handler"
    - path: "backend/test/application/network_scenarios_test.clj"
      provides: "GWT scenarios for get-network-detail"
      min_lines: 40
  key_links:
    - from: "backend/src/application/network_scenarios.clj"
      to: "backend/src/domain/production.clj"
      via: "production/find-by-network-id call"
      pattern: "production/find-by-network-id"
    - from: "backend/src/infrastructure/rest_api/network_handler.clj"
      to: "backend/src/application/network_scenarios.clj"
      via: "scenarios/get-network-detail call"
      pattern: "scenarios/get-network-detail"
    - from: "backend/src/infrastructure/rest_api/handler.clj"
      to: "backend/src/infrastructure/rest_api/network_handler.clj"
      via: "network-handler/routes call with production-repo + consumption-repo"
      pattern: "network-handler/routes.*production-repo.*consumption-repo"
---

<objective>
Backend API endpoint for network detail page: add `find-by-network-id` to ProductionRepo, create the aggregation scenario, wire the handler, and test everything.

Purpose: Establish the data contract that the frontend will consume. Security-critical: the public endpoint must whitelist fields to prevent sensitive data leaks.
Output: Working `GET /api/v1/networks/:id/detail` endpoint with tests.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-infrastructure/01-RESEARCH.md
@.planning/phases/01-infrastructure/01-VALIDATION.md
@backend/src/domain/production.clj
@backend/src/domain/network.clj
@backend/src/domain/consumption.clj
@backend/src/application/network_scenarios.clj
@backend/src/infrastructure/rest_api/network_handler.clj
@backend/src/infrastructure/rest_api/handler.clj
@backend/src/infrastructure/xtdb/xtdb_production_repo.clj
@backend/src/infrastructure/in_memory_repo/mem_production_repo.clj

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->

From backend/src/domain/production.clj (line 170):
```clojure
(defprotocol ProductionRepo
  (find-by-id      [repo id]                    "Find a production by ID.")
  (find-by-user-id [repo user-id]               "Find all productions for a user. Returns a vector."))
;; ADD: (find-by-network-id [repo network-id] "Find all productions for a network.")
```

From backend/src/domain/consumption.clj (line 190):
```clojure
(defprotocol ConsumptionRepo
  ...
  (count-by-network-id [repo network-id]        "Count consumptions linked to a network."))
```

From backend/src/infrastructure/rest_api/handler.clj (line 20):
```clojure
(defn- build-router [network-repo user-repo consumption-repo production-repo ec-repo token-verifier ...]
  ...
  (network-handler/routes network-repo ec-repo)  ;; MUST add production-repo consumption-repo
```

From backend/src/infrastructure/rest_api/network_handler.clj (line 45):
```clojure
(defn routes [network-repo ec-repo] ...)  ;; MUST extend signature
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add find-by-network-id to ProductionRepo + implementations</name>
  <files>backend/src/domain/production.clj, backend/src/infrastructure/xtdb/xtdb_production_repo.clj, backend/src/infrastructure/in_memory_repo/mem_production_repo.clj</files>
  <behavior>
    - find-by-network-id returns a vector of productions matching the given network-id
    - find-by-network-id returns an empty vector when no productions exist for the network
    - find-by-network-id returns full production entities (filtering is done at the scenario layer)
  </behavior>
  <action>
1. In `backend/src/domain/production.clj`, add `(find-by-network-id [repo network-id] "Find all productions for a network. Returns a vector.")` to the `ProductionRepo` protocol (after `find-by-user-id`).

2. In `backend/src/infrastructure/xtdb/xtdb_production_repo.clj`, implement `find-by-network-id` following the exact pattern of `find-by-user-id`:
```clojure
(find-by-network-id [_ network-id]
  (let [results (xt/q (xt/db node)
                      '{:find  [(pull e [*])]
                        :where [[e :production/network-id nid]]
                        :in    [nid]}
                      network-id)]
    (mapv (fn [[doc]] (doc->production doc)) results)))
```

3. In `backend/src/infrastructure/in_memory_repo/mem_production_repo.clj`, implement `find-by-network-id` following the `find-by-user-id` pattern:
```clojure
(find-by-network-id [_ network-id]
  (vec (keep (fn [p]
               (when (= network-id (:production/network-id p))
                 (production/build-production p)))
             (vals @store))))
```

4. Run existing tests to ensure no regression.
  </action>
  <verify>
    <automated>cd backend && clj -M:test</automated>
  </verify>
  <done>ProductionRepo protocol has find-by-network-id, both XTDB and in-memory implementations pass existing tests, no regressions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create get-network-detail scenario + handler + tests</name>
  <files>backend/src/application/network_scenarios.clj, backend/src/infrastructure/rest_api/network_handler.clj, backend/src/infrastructure/rest_api/handler.clj, backend/test/application/network_scenarios_test.clj</files>
  <behavior>
    - get-network-detail for a public network returns {:network ..., :productions [...], :consumer-count N, :total-capacity-kwc N, :energy-mix {...}}
    - get-network-detail for a non-public network throws ex-info (handler returns 404)
    - get-network-detail for a non-existent network throws ex-info (handler returns 404)
    - get-network-detail for a network with zero active productions returns total-capacity-kwc 0 and energy-mix {}
    - Serialized network contains ONLY: :network/id, :network/name, :network/center-lat, :network/center-lng, :network/radius-km, :network/lifecycle
    - Serialized productions contain ONLY: :production/id, :production/energy-type, :production/installed-power, :production/producer-address
    - Response never contains :production/iban, :production/pdl-prm, :production/user-id
  </behavior>
  <action>
1. **Write tests first** in `backend/test/application/network_scenarios_test.clj`. Add GWT scenarios using the project's `bdd.test-gwt` macro and in-memory repos:
   - GIVEN a public network with 2 active productions (solar 3kWc, wind 5kWc) and 3 consumers WHEN get-network-detail THEN returns correct aggregation with total-capacity-kwc=8, energy-mix={:solar 50.0, :wind 50.0}, consumer-count=3
   - GIVEN a non-public (private) network WHEN get-network-detail THEN throws ex-info
   - GIVEN a non-existent network-id WHEN get-network-detail THEN throws ex-info
   - GIVEN a public network with zero active productions WHEN get-network-detail THEN returns total-capacity-kwc=0, energy-mix={}, empty productions vector
   - GIVEN a public network with productions WHEN get-network-detail THEN response contains no :production/iban, :production/pdl-prm, :production/user-id keys (use `every?` + `complement` over `(keys)` of each serialized production)
   Tag all tests with `^:phase-1` metadata.

2. **Run tests — they must fail (RED).**

3. **Implement `get-network-detail`** in `backend/src/application/network_scenarios.clj`:
   - Add `[domain.production :as production]` to the ns requires (if not already present).
   - Add private helpers `serialize-public-network` and `serialize-public-production` using `select-keys` whitelists (see RESEARCH.md Pattern 2).
   - Add `get-network-detail [network-repo production-repo consumption-repo network-id]` function (see RESEARCH.md Pattern 1). Guard non-public with `(not= :public (:network/lifecycle network))`. Guard zero productions for energy-mix with `(when (seq active-prods) ...)`.

4. **Add handler** in `backend/src/infrastructure/rest_api/network_handler.clj`:
   - Add `[application.network-scenarios :as scenarios]` to ns requires.
   - Add `get-network-detail-handler [network-repo production-repo consumption-repo]` function that extracts `:id` from path-params, calls `scenarios/get-network-detail`, catches `ExceptionInfo` and returns 404 (see RESEARCH.md handler example).
   - Extend `routes` function signature from `[network-repo ec-repo]` to `[network-repo ec-repo production-repo consumption-repo]`. Add route `["/networks/:id/detail" {:get {:handler (get-network-detail-handler network-repo production-repo consumption-repo)}}]` under the existing `/networks` routes.

5. **Update wiring** in `backend/src/infrastructure/rest_api/handler.clj`:
   - Change line ~24 from `(network-handler/routes network-repo ec-repo)` to `(network-handler/routes network-repo ec-repo production-repo consumption-repo)`. Both repos are already available in the `build-router` signature.

6. **Run tests — they must pass (GREEN).**
  </action>
  <verify>
    <automated>cd backend && clj -M:test</automated>
  </verify>
  <done>All GWT scenarios pass. GET /api/v1/networks/:id/detail returns correct aggregation for public networks, 404 for non-public/missing, zero sensitive fields in response. Full test suite green.</done>
</task>

</tasks>

<verification>
```bash
# Full backend test suite
cd backend && clj -M:test

# Phase-specific tests
cd backend && clj -M:test --focus-meta :phase-1
```
</verification>

<success_criteria>
1. `find-by-network-id` exists on ProductionRepo protocol with both implementations
2. `get-network-detail` scenario aggregates network + productions + stats
3. Handler returns 200 for public network, 404 for non-public/missing
4. JSON response contains zero sensitive fields (no IBAN, PRM, user-id)
5. All backend tests pass (existing + new)
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure/01-01-SUMMARY.md`
</output>
